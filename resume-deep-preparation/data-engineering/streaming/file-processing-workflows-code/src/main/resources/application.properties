# Reporting store: use file-backed so API loads data written by PipelineRunner
# Run from project root (file-processing-workflows-code); output is relative to CWD
reporting.store.path=output/reporting

server.port=8080
